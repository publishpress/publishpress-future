name: Unit and Integration Tests
permissions:
  contents: read
on:
  pull_request:
    branches:
      - 'development'
      - 'release/*'
      - 'release-*'

  workflow_call:

  workflow_dispatch:

jobs:
  check:
    # v2.0.0
    uses: publishpress/github-workflows/.github/workflows/unit-tests.yml@master
