name: Deploy assets/readme to WordPress.org
permissions:
  contents: read
on:
  push:
    branches:
      - 'master'

jobs:
  deploy-to-trunk:
    uses: publishpress/github-workflows/.github/workflows/deploy-free-assets.yml@ubuntu-latest
